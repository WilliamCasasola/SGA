@model IEnumerable<SGA.Models.Facturado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estudiante.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estudiante.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FacturadoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FacturadoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FacturadoId })
        </td>
    </tr>
}

</table>
